apiVersion: v1
kind: Secret
metadata:
  name: zapier-webhook-secrets
type: Opaque
stringData:
  # Fill these before applying (kubectl will base64-encode automatically)
  OPENAI_API_KEY: ""
  # Optional: Only if you plan to use ngrok inside the pod (generally not needed with Ingress)
  NGROK_AUTHTOKEN: ""


